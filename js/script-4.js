/**** Методи масиву ****/

// Метод split(delimiter)

// Метод join(delimiter)

// Метод indexOf(value)

// Метод includes(value)

// Метод push(newValue)

// Метод pop()

// Метод slice(begin, end)

// Метод splice(position, num)

// Додавання методом splice(position, 0, new_element_1, new_element_2, ...)

// Заміна методом splice(position, num, new_element_1, new_element_2, ...)

// Метод concat(newArray)
